version = 1
SPDX-PackageName = "awfy"
SPDX-PackageSupplier = "Lukas Backström <lukas@erlang.org>"

# Rule (LICENSING_POLICY.md): every file carries its own inline SPDX
# header so copies preserve the licence. Bulk annotations here are
# reserved for files that *cannot* take a comment. Keep this list
# tight — a new entry should answer "what comment syntax does this
# file have?" with "none" before it lands here.

# mix.lock — generated by `mix deps.get`; the file is a single
# Elixir map that mix rewrites wholesale, so a leading comment
# wouldn't survive the next dependency change. CC0 ("no rights
# reserved") rather than Apache-2.0 because the file is purely
# derived metadata, not authored source.
[[annotations]]
path = "mix.lock"
precedence = "aggregate"
SPDX-FileCopyrightText = "NONE"
SPDX-License-Identifier = "CC0-1.0"

# Embedded test data for the Json benchmark, copied from the
# upstream AWFY repo (Stefan Marr, MIT — see LICENSING_POLICY.md).
# JSON has no comment syntax, so the SPDX info has to live here.
[[annotations]]
path = "apps/awfy/priv/rap_benchmark.json"
precedence = "aggregate"
SPDX-FileCopyrightText = "Stefan Marr"
SPDX-License-Identifier = "MIT"

# JSON config for the JS/CSS linters that run against
# priv/dashboard.{js,css}. JSON has no comment syntax, so SPDX has
# to live here. package-lock.json is generated by `npm install`.
[[annotations]]
path = ["package.json", "package-lock.json", ".stylelintrc.json"]
precedence = "aggregate"
SPDX-FileCopyrightText = "2026 Lukas Backström <lukas@erlang.org>"
SPDX-License-Identifier = "Apache-2.0"

# Vendored target-Elixir deps under apps/awfy_target_runner/deps/.
# Verbatim copies of the upstream Hex packages (the only edits are
# the dev/test strip in each `mix.exs` per
# PLAN/TARGET_ELIXIR_RUNNER_PLAN.md Appendix A — those are
# derivative changes, not new authorship). Each upstream tree
# carries its own LICENSE.md, but REUSE wants per-file SPDX, so
# annotate the whole tree here rather than graffiti vendor sources.
[[annotations]]
path = "apps/awfy_target_runner/deps/benchee/**"
precedence = "aggregate"
SPDX-FileCopyrightText = "2016 Tobias Pfeiffer"
SPDX-License-Identifier = "MIT"

[[annotations]]
path = "apps/awfy_target_runner/deps/deep_merge/**"
precedence = "aggregate"
SPDX-FileCopyrightText = "2016 Tobias Pfeiffer"
SPDX-License-Identifier = "MIT"

[[annotations]]
path = "apps/awfy_target_runner/deps/statistex/**"
precedence = "aggregate"
SPDX-FileCopyrightText = "2016 Tobias Pfeiffer"
SPDX-License-Identifier = "MIT"

# mix.lock files inside the vendored sub-app — generated by `mix
# deps.get` against the path-only deps, same rationale as the root
# mix.lock above (CC0, derived metadata).
[[annotations]]
path = "apps/awfy_target_runner/mix.lock"
precedence = "aggregate"
SPDX-FileCopyrightText = "NONE"
SPDX-License-Identifier = "CC0-1.0"

# `mix compile` and `mix test` artifacts in the sub-app's _build/.
# Already gitignored, but `reuse lint` walks the working tree and
# doesn't reliably honor gitignore for nested project trees, so
# annotate explicitly. CC0 because every file here is wholly
# derived (.beam files, .mix metadata, consolidated protocols).
# Same logic for vendored deps' own _build/ if mix ever creates it.
[[annotations]]
path = ["apps/awfy_target_runner/_build/**", "apps/awfy_target_runner/deps/**/_build/**"]
precedence = "aggregate"
SPDX-FileCopyrightText = "NONE"
SPDX-License-Identifier = "CC0-1.0"
